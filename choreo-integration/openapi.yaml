openapi: "3.0.0"
info:
  title: "Aegis Disaster Alert API"
  version: "1.0.0"
paths:
  /aegis/alert:
    post:
      summary: "Receive disaster alert from Supabase"
      operationId: "receiveAlert"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: "OK"
